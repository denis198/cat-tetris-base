# 🚀 Ручной запуск деплоя на Vercel

## Вариант 1: Через Vercel Dashboard (Рекомендуется)

1. **Откройте Vercel Dashboard:**
   - Перейдите на https://vercel.com/popovdenisn-1952s-projects/cat-tetris-base
   - Или https://vercel.com/dashboard

2. **Найдите ваш проект:**
   - Найдите проект `cat-tetris-base` в списке

3. **Запустите деплой вручную:**
   - Нажмите на проект
   - Перейдите на вкладку **"Deployments"**
   - Нажмите кнопку **"Redeploy"** или **"Deploy"**
   - Выберите последний коммит (или ветку `main`)
   - Нажмите **"Redeploy"**

## Вариант 2: Через Vercel CLI

Если у вас установлен Vercel CLI:

```bash
# Установите Vercel CLI (если еще не установлен)
npm install -g vercel

# Войдите в Vercel
vercel login

# Запустите деплой
cd /Users/denispopov/Downloads/cat-tetris-base
vercel --prod
```

## Вариант 3: Проверка настроек автоматического деплоя

Если автоматический деплой не работает, проверьте настройки:

1. **В Vercel Dashboard:**
   - Откройте проект → **Settings** → **Git**
   - Убедитесь, что репозиторий подключен: `denis198/cat-tetris-base`
   - Проверьте, что выбрана правильная ветка: `main`
   - Убедитесь, что **"Auto-deploy"** включен

2. **В GitHub:**
   - Откройте репозиторий: https://github.com/denis198/cat-tetris-base
   - Перейдите в **Settings** → **Webhooks**
   - Проверьте, что есть webhook от Vercel
   - Если нет - Vercel должен создать его автоматически при подключении

3. **Переподключение репозитория:**
   - В Vercel Dashboard → Settings → Git
   - Нажмите **"Disconnect"** и затем **"Connect Git Repository"**
   - Выберите `denis198/cat-tetris-base`
   - Настройте проект заново:
     - Framework Preset: **Other**
     - Root Directory: `.` (точка)
     - Build Command: (оставьте пустым)
     - Output Directory: `.` (точка)
     - Install Command: (можно оставить пустым)

## Вариант 4: Создать пустой коммит для триггера

Иногда помогает создать пустой коммит, чтобы триггернуть деплой:

```bash
cd /Users/denispopov/Downloads/cat-tetris-base
git commit --allow-empty -m "Trigger Vercel deployment"
git push origin main
```

## Проверка после деплоя

После запуска деплоя:

1. Проверьте статус в Vercel Dashboard
2. Дождитесь завершения деплоя (обычно 1-2 минуты)
3. Откройте URL проекта
4. Проверьте логи деплоя на наличие ошибок

## Если деплой все еще не работает

1. Проверьте логи деплоя в Vercel Dashboard
2. Убедитесь, что все файлы на месте в репозитории
3. Проверьте, что `vercel.json` правильно настроен
4. Попробуйте удалить проект в Vercel и создать заново
