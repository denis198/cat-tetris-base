# 🔧 Исправление ошибки 404

## Проблема
После успешного деплоя (без ошибки Next.js) появляется ошибка 404 - файлы не находятся.

## Решение

### Что было исправлено:

1. ✅ **Упрощен `vercel.json`** - убрана конфигурация `builds`, которая могла вызывать проблемы
2. ✅ Используется только `rewrites` для правильной маршрутизации файлов
3. ✅ Сохранены необходимые заголовки для CORS

### Новая конфигурация `vercel.json`:

```json
{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/$1"
    }
  ],
  "headers": [...]
}
```

## Что делать дальше:

1. **Загрузите исправления:**
   ```bash
   git add vercel.json
   git commit -m "Fix: исправлена конфигурация для устранения 404"
   git push origin main
   ```

2. **Проверьте деплой:**
   - После push Vercel автоматически запустит новый деплой
   - Дождитесь завершения (обычно 1-2 минуты)
   - Откройте URL проекта

3. **Проверьте доступность файлов:**
   - `https://ваш-url.vercel.app/` - должна открыться главная страница
   - `https://ваш-url.vercel.app/index.html` - также должна работать
   - `https://ваш-url.vercel.app/tetris.js` - должен загружаться скрипт
   - `https://ваш-url.vercel.app/style.css` - должен загружаться стиль

## Если 404 все еще появляется:

1. **Проверьте настройки в Vercel Dashboard:**
   - Settings → General → Output Directory должно быть `.` (точка)
   - Framework Preset должен быть "Other"

2. **Проверьте логи деплоя:**
   - В Vercel Dashboard → Deployments → выберите последний деплой
   - Проверьте Build Logs на наличие ошибок

3. **Попробуйте очистить кэш:**
   - В Vercel Dashboard → Deployments
   - Нажмите на три точки рядом с деплоем → "Redeploy"
   - Выберите "Use existing Build Cache" = НЕТ

## Проверка структуры файлов:

Убедитесь, что в корне проекта есть:
- ✅ `index.html`
- ✅ `tetris.js`
- ✅ `style.css`
- ✅ `farcaster.json`
- ✅ `vercel.json`

Все эти файлы должны быть в корне репозитория (не в подпапках).
