# 🔧 Исправления для деплоя

## Исправленные проблемы:

1. ✅ **vercel.json** - добавлена явная конфигурация для статических файлов с `@vercel/static`
2. ✅ **package.json** - добавлены пустые `dependencies` и `devDependencies` для явного указания, что это не Next.js проект
3. ✅ Исправлена ошибка "No Next.js version detected" - Vercel теперь знает, что это статический проект
4. ✅ Все JSON файлы проверены на валидность

## Что было исправлено:

### vercel.json
- Добавлена явная конфигурация `@vercel/static` для статических файлов
- Это предотвращает автоматическое определение Next.js
- Настроены правильные routes для всех файлов
- Оставлены необходимые заголовки для CORS и безопасности

### package.json
- Добавлены пустые `dependencies` и `devDependencies`
- Это явно указывает Vercel, что это не Next.js проект
- Оставлены только рабочие скрипты для разработки

## Команды для деплоя:

```bash
# 1. Проверьте изменения
git status

# 2. Добавьте все файлы
git add .

# 3. Закоммитьте исправления
git commit -m "Fix: исправлена конфигурация для Vercel деплоя"

# 4. Запушьте на GitHub
git push origin main
```

После push Vercel автоматически запустит новый деплой с исправлениями.

## Настройки Vercel:

В настройках проекта Vercel (Dashboard → Settings → General):
- **Framework Preset**: Other (или оставьте автоматическое определение)
- **Build Command**: (оставьте пустым - не нужен для статических файлов)
- **Output Directory**: `.` (точка - корневая директория)
- **Install Command**: (можно оставить пустым)

**Важно:** Теперь `vercel.json` явно указывает использование `@vercel/static`, поэтому Vercel не будет пытаться определить Next.js.

## Проверка после деплоя:

1. Откройте URL вашего проекта на Vercel
2. Проверьте, что игра загружается: `https://ваш-url.vercel.app`
3. Проверьте консоль браузера (F12) на наличие ошибок
4. Убедитесь, что все файлы доступны:
   - `https://ваш-url.vercel.app/index.html`
   - `https://ваш-url.vercel.app/tetris.js`
   - `https://ваш-url.vercel.app/style.css`
   - `https://ваш-url.vercel.app/farcaster.json`
